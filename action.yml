name: Firecover coverage uploader
description: Github action to upload coverage to Firecover
author: Vajahath Ahmed
inputs:
  appToken:
    required: false
    description: 'Repository upload token - get it from Firecover'
runs:
  using: 'node16'
  main: 'dist/index.js'
